package com.cg.OopsInJava;


import java.text.ParseException;

public class StudentMain {

	public static void main(String[] args) {

		/*
		 * Student st=new Student(); st.readStudentDetails();
		 * st.displayStudentDetails();
		 */
		/*
		 * Employee em=new Employee(); em.readEmployeeDetails();
		 * em.displayEmployeeDetails();
		 */
		//creating array of object
		/*
		 * Employee[] listOfEmployees=new Employee[2]; Employee employee; //creating and
		 * adding the employee object in the above array for(int
		 * i=0;i<listOfEmployees.length;i++) { employee = new Employee();
		 * employee.readEmployeeDetails(); listOfEmployees[i]=employee;
		 * 
		 * } for(int i=0;i<listOfEmployees.length;i++) {
		 * System.out.println(listOfEmployees[i]);
		 * listOfEmployees[i].displayEmployeeDetails(); }
		 */
		/*
		 * PrintMarks[] listOfStudents=new PrintMarks[8]; PrintMarks printMarks; for(int
		 * i=0;i<listOfStudents.length;i++) { printMarks=new PrintMarks();
		 * printMarks.readMarks(); listOfStudents[i]=printMarks; } for(int
		 * i=0;i<listOfStudents.length;i++) { System.out.println(listOfStudents[i]);
		 * listOfStudents[i].displayMarks(); }
		 */
		
		AverageHight[] hightOfStudents= new AverageHight[1];
		AverageHight[] average;
		System.out.println("String".substring(0,4));
	}


}